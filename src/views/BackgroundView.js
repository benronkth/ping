function BackgroundView({ height, width }) {
    return (<div className="background" style={{
        height: height,
        width: width
    }}>
    </div>);
}

export default BackgroundView;
